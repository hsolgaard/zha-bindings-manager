{
  "name": "zha-bindings-manager",
  "private": true,
  "version": "0.34.1",
  "description": "Build tooling for the ZHA Bindings Manager Lovelace card — bundles src/index.js into the single zha-binding-map-card.js Home Assistant loads.",
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch",
    "test": "vitest run"
  },
  "dependencies": {
    "esbuild": "^0.28.1"
  },
  "devDependencies": {
    "vitest": "^4.1.10"
  }
}
