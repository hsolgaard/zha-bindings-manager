// Unused scratch file — content was already copied into utils.js during the
// M1 module split; the sandbox wouldn't allow deleting this one (filesystem
// lock, not a code issue). Not imported anywhere.
